[project]
name = "pdf-sales-reports"
version = "2.0.0"
description = "Генерация PDF-отчётов по продажам: pandas + matplotlib + reportlab + Telegram"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
